# filebeat.config:
#   modules:
#     path: ${path.config}/modules.d/*.yml
#     reload.enabled: false

# filebeat.autodiscover:
#   providers:
#     - type: docker
#       hints.enabled: true

filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /logs/nginx/*.log

output.elasticsearch:
  hosts:
    - es01:9200
  username: XXXXXXXXX
  password: XXXXXXXXX
